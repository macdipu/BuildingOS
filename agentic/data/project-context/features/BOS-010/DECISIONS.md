# BOS-010 decision register

Status: RESOLVED at REQUIREMENTS level. D-01/D-02 carry forward from BOS-002 as
pre-confirmed candidates. D-07/D-08 are now resolved by the operator. D-09/D-10 remain
technical-design inputs for TECHNICAL, not REQUIREMENTS blockers.

| ID | Decision needed | Known source constraint | Needed before |
|---|---|---|---|
| D-01 (carried) | Development OTP: `DevelopmentOtpProvider` accepts `000000` for active challenges, local/test only, behind a replaceable adapter. Phone-only login working interpretation; Google deferred. | BOS-002 operator decision, 2026-09-22; unaffected by the platform-role/SaaS revision | Login/session implementation |
| D-02 (carried) | First `SUPER_ADMIN` is provisioned by a deploy-time seed script/config (seed phone `01306999005`); authenticates via normal phone+OTP; no admin panel or password. Consistent with the revised model's platform-scoped roles (§110.1) since `SUPER_ADMIN` is explicitly platform-scoped, not building-scoped. | BOS-002 operator decision, 2026-09-22 | Platform-role bootstrap implementation |
| D-07 | RESOLVED: `PLATFORM_ADMIN` has full approve/reject/request-information/activate/suspend authority over building applications, the same as `SUPER_ADMIN` — no value/size threshold, no forced escalation. Both roles are checked identically by the approval use case. | Operator decision, 2026-09-22; §4.1, §149.4-149.7 | Building-application approval use case authorization check |
| D-08 | RESOLVED: subscription plans are **admin-configurable, not hardcoded**. No fixed MVP plan catalog is seeded/shipped. An authorized back-office role (`SUBSCRIPTION_ADMIN`/`PLATFORM_ADMIN`/`SUPER_ADMIN` per D-07-style checks) creates/edits `SubscriptionPlan` records (name, `unit_limit`, `user_limit`, `storage_limit`, enabled features, trial length) through the back-office plan CRUD screens/APIs already specified at §149.19 (`POST /api/v1/platform/subscription-plans`). Bootstrapping consequence: at least one plan must exist, created by a back-office operator, before any building application can be approved into `ONBOARDING` with an attached subscription (BA-03) — this is an operational prerequisite, not a hardcoded seed value. | Operator decision, 2026-09-22; §8.12, §110.3, §149.15-149.16,§149.19,§149.23 | Subscription plan CRUD implementation; back-office plan-management screen; approval flow's "attach initial subscription" step must handle "no plan configured yet" as an explicit blocked/error state, never a fabricated default |
| D-09 | Back-office web app technology. BRD suggests Flutter Web "to stay aligned with the primary client stack" but explicitly does not mandate it (§149.1). | §149.1 | Technical design / TECHNICAL stage, not a REQUIREMENTS blocker |
| D-10 | Exact assisted-onboarding/support-session `access_scope`/`permission_scope` enumeration and approval workflow for elevated support actions. BRD already specifies the entities, statuses and restrictions in detail (§149.11-149.12); only the precise scope-value enumeration and elevated-approval routing remain open. | §149.11-149.12 | Authorization design for onboarding/support agents |
| D-11 | RESOLVED: sequencing — subscription plans (G5, SUB-03 plan CRUD) are built **before** the building-application slice, so approval can attach a plan exactly per BA-03/§149.7 step 6. The building-application run was started and cancelled at INTAKE for this reason. | Operator decision, 2026-09-23 | Order of feature slices |
| D-12 | RESOLVED: initial `BUILDING_ADMIN` on approval is chosen by the approver; the approve request carries the admin's phone, and the review form pre-fills the applicant's phone as the default (reviewer may keep or change it). Consistent with §149.3 ("applicant does not automatically become Building Admin"). | Operator decision, 2026-09-23 | Approve use case / review screen |
| D-13 | RESOLVED: building-application slice persists an audited transition row for every state change; Kafka lifecycle events (§149.20) via a transactional outbox are a later slice. | Operator decision, 2026-09-23 | Building-application persistence; later outbox slice |
| D-14 | RESOLVED: building-application slice also includes duplicate-building review signals (§149.6), verification document upload (storage technology still to be decided at TECHNICAL), a Flutter customer submit screen, and ACTIVE ⇄ SUSPENDED operational actions. | Operator decision, 2026-09-23 | Building-application scope |
| D-15 | SUPERSEDED by D-22..D-27 —: first subscription slice = plan CRUD + "start trial" building-subscription action only; other §149.15 subscription actions in a later slice. | Operator decision, 2026-09-23 | F5a scope |
| D-16 | RESOLVED: plan entitlements are the fixed §149.16 key list; unknown keys rejected; new keys need a code change. | Operator decision, 2026-09-23 | Plan validation |
| D-17 | RESOLVED: plan billing-cycle options are `MONTHLY`, `QUARTERLY`, `YEARLY` (labels only; no billing in MVP). | Operator decision, 2026-09-23 | Plan model |
| D-18 | RESOLVED: subscription data lives in a new `subscription-service` microservice with its own `subscription_db` (per BRD), not inside building-service. | Operator decision, 2026-09-23 | Service topology |
| D-19 | SUPERSEDED by D-22..D-27 —: `maintenance.enabled` is always free for every building; other features are locked behind plan entitlements. No trial for now: attaching a plan starts the subscription `ACTIVE`; trials arrive with paid plans later. | Operator answers, 2026-09-23 | Entitlement resolution; attach-plan use case |
| D-20 | RESOLVED: plan edits (limits/features) apply immediately to all subscriptions on that plan; edits are audited. | Operator decision, 2026-09-23 | Effective-entitlement read path |
| D-21 | SUPERSEDED by D-22..D-27 — (BRD deviation): a subscription can cover one building (default) or several buildings grouped by an admin, with shared limits. Data model supports it from the first slice; grouping API/pooled limits are the next slice. | Operator decision, 2026-09-23; deviates from §110.3 default and §149.23 MVP boundary | Subscription model |
| D-22 | RESOLVED (BRD deviation): subscriptions are **per user**, not per building. One user subscription covers every building the user belongs to. Each user unlocks paid features only for themselves (a subscribed owner does not unlock features for a tenant). Deviates from BRD §110.3/§149.15 (building-level). | Operator decision, 2026-09-23 | Subscription model, entitlement resolution |
| D-23 | RESOLVED: free tier = maintenance for every user; all other features require an active user subscription. A new building member is on the free tier until they subscribe. Which features are free is admin-editable data (D-27). | Operator decision, 2026-09-23 | Entitlement resolution |
| D-24 | RESOLVED: creating a building requires a one-time building-creation fee. For now it is paid offline and a back-office admin records it (amount, reference, date); building-application approval is blocked until recorded. Payment-gateway collection later (BOS-004). | Operator decision, 2026-09-23 | Fee schedule, payment record, approval precondition |
| D-25 | RESOLVED: user subscriptions are granted both by a back-office admin assigning a plan and by the user self-subscribing in the app; no charge for now. | Operator decision, 2026-09-23 | Grant use cases |
| D-26 | RESOLVED: approval no longer attaches a subscription (replaces BA-03 step 6 / §149.7 step 6); it requires the creation fee to be recorded (D-24). D-11 sequencing still holds: this revenue slice precedes building application. | Operator decision, 2026-09-23 | Building-application approval |
| D-27 | RESOLVED: the revenue model must be changeable without redesign — subscriber type, free-tier features, fee amounts and whether a fee is required are configuration/data; entitlement resolution and payment collection sit behind replaceable ports. | Operator instruction, 2026-09-23 | Whole F5a design |

D-09 and D-10 are technical-design inputs, not REQUIREMENTS blockers (same treatment as
D-04-D-06 in the superseded BOS-002 register).

No credentials are recorded in this document.
