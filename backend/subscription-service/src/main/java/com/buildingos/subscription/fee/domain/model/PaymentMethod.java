package com.buildingos.subscription.fee.domain.model;

/** How a fee was paid. {@code MANUAL} = recorded by an admin after offline payment; gateways add values later. */
public enum PaymentMethod { MANUAL }
