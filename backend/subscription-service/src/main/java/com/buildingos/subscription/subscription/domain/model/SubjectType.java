package com.buildingos.subscription.subscription.domain.model;

/** Who a subscription belongs to. {@code USER} today (D-22); other subject types can be added later. */
public enum SubjectType { USER }
