package com.buildingos.building.building.domain.model;

/** Operational lifecycle after approval (BRD 149.4). ARCHIVED arrives in a later slice. */
public enum BuildingStatus { ONBOARDING, ACTIVE, SUSPENDED }
