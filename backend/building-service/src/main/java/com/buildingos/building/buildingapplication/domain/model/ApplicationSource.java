package com.buildingos.building.buildingapplication.domain.model;

/** Only SELF_SERVICE this slice; the others of BRD 149.3 arrive with assisted/back-office onboarding. */
public enum ApplicationSource { SELF_SERVICE }
