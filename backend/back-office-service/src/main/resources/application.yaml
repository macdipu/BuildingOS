server:
  port: 8080
  error:
    include-message: never
    include-stacktrace: never
platform:
  security:
    issuer: ${JWT_ISSUER}
    audience: ${JWT_AUDIENCE}
    jwk-set-uri: ${JWT_JWK_SET_URI}
    allowed-origins: ${CORS_ALLOWED_ORIGINS:}
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
      group:
        liveness:
          include: livenessState
        readiness:
          include: readinessState,db
logging:
  structured:
    format:
      console: logstash
spring:
  application:
    name: back-office-service
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    hikari:
      initialization-fail-timeout: -1
  flyway:
    enabled: true
    clean-disabled: true
    validate-on-migrate: true
buildingos:
  services:
    auth-url: ${AUTH_SERVICE_URL}
    building-url: ${BUILDING_SERVICE_URL}
    connect-timeout: ${SERVICE_CONNECT_TIMEOUT:PT2S}
    read-timeout: ${SERVICE_READ_TIMEOUT:PT5S}
  onboarding:
    max-duration: ${ONBOARDING_MAX_DURATION:P30D}
  support:
    max-duration: ${SUPPORT_MAX_DURATION:PT8H}
